import React from "react";
import { Route, BrowserRouter, Routes } from "react-router-dom";

const Rotas = () => {
    <BrowserRouter>
        <Routes>
            
        </Routes>
    </BrowserRouter>
}